[package]
name = "muscle_rs"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.40", features = ["derive"] }
tracehash = { package = "tracehash-rs", version = "0.2.0", optional = true }

[features]
default = []
tracehash = ["dep:tracehash"]
